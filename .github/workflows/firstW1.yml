name: First Workflow

on: push

jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "myfirst github action"

        - name: List Files
          run: echo

        - name: Read File
          run: cat README.md
