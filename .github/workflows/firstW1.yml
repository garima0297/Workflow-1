name: First Workflow

on: push

jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v7

        - name: Welcome message
          run: echo "myfirst github action"

        - name: List Files
          run: ls

        - name: Read File
          run: cat README.md
